{% extends 'base.html.twig' %}

{% block title %}Annonces - Agence immobilière{% endblock %}

{% block body %}
{% for message in app.flashes('message_alert') %}
{% endfor %}
	<section class="properties-section">
    {% if offerCount is defined and offerCount > 0 %}
			<div class="property-counter">Vous avez {{ offerCount }}{{ offerCount > 1 ? ' annonces enregistrées' : ' annonce enregistrée'}} dans vos favoris</div>
		{% endif %}
		<div class="properties-container">
			{% for offer in offers %}
				{{ include('components/_card_property.html.twig') }}
			{% else %}
				<div class="no-property-to-display">😓 Il n'y a encore aucune annonce en favoris à afficher.</div>
			{% endfor %}
			<div class="card-show-more">
				<a href="{{ path('offer_home') }}">afficher<br>toutes les<br>annonces
				</a>
			</div>
		</div>
	</section>
	{% if offers|length > 0 %}
    {% if offers.getTotalItemCount > 10 %}
      {{ knp_pagination_render(offers) }}
    {% endif %}
  {% endif %}
{% endblock %}

